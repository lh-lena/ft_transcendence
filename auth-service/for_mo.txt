could you add these columns/fields to the user table:

. Storage format
twofa_temp_code: store as a string (6-digit code).
twofa_code_expires: store as ISO timestamp (string) or a datetime column.
twofa_secret: store the base32 secret string from otplib.

backup_codes:
Best stored as an array of SHA-256 hashes (never plaintext).
If DB doesn’t support arrays, store JSON string.

is_2fa_enabled: boolean; ->needs to be a boolean

{
  is_2fa_enabled: boolean;       // true/false if 2FA is enabled
  twofa_method: 'email' | 'totp' | null;   // which method
  twofa_temp_code?: string | null;         // email codes (short-lived)
  twofa_code_expires?: string | null;      // expiry timestamp for email code
  twofa_secret?: string | null;            // TOTP shared secret
  backup_codes?: string[] | null;          // hashed backup codes
}


Update backend routes
GET /user and GET /user/:id
must return those new fields so the auth-service can check 2FA state.

PUT /user/:id
must accept updates to those fields (is_2fa_enabled, twofa_secret, backup_codes, etc.).

POST /user
 when creating a new user, initialize defaults:

JWT Secret for refresh tokens: REFRESH_TOKEN_SECRET
