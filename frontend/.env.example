# Vite Build-time Environment Variables
# These are injected at BUILD time, not runtime

# Auth Service URL (for API calls)
VITE_AUTH_URL=/auth

# WebSocket URL (for real-time communication)
VITE_WEBSOCKET_URL=wss://localhost/ws
