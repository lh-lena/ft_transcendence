version: '3.8'
services:
  # Remove health check dependencies from nginx
  nginx:
    depends_on:
      - backend
      - frontend
      - realtime
      - auth-service
  
  # Disable monitoring
  prometheus:
    profiles: [monitoring]
  grafana:
    profiles: [monitoring]
  nginx-exporter:
    profiles: [monitoring]
